export enum RoleStatus {
    'NORMAL' = 1,
    'DEL' = 2
}

export enum UserStatus {
    'NORMAL' = 1,
    'LOCK' = 2,
    'EXPIRY' = 3,
    'DEL' = 4
}

export enum LevelStatus {
    'NORMAL' = 1,
    'DEL' = 2
}
